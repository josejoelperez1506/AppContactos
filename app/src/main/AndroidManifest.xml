<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.joseperez.appcontactos">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".EliminarContacto" />
        <activity android:name=".AgregarContacto" />
        <activity android:name=".ModificarContacto" />
        <activity android:name=".BuscarContacto" />
        <activity android:name=".PreModificarContacto"></activity>
    </application>

</manifest>